<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <ImageView
        android:id="@+id/computer_image"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:scaleType="fitCenter"

        android:src="@drawable/ic_desktop_windows_black_24dp"
        android:layout_marginRight="8dp"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginLeft="8dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp" />

    <TextView
        android:id="@+id/computer_name"
        android:layout_width="82dp"
        android:layout_height="55dp"
        android:text="@string/dummy_computer_name"
        android:textSize="@dimen/text_primary"
        app:layout_constraintLeft_toLeftOf="@+id/computer_image"
        app:layout_constraintTop_toBottomOf="@+id/computer_image"
        android:layout_marginLeft="0dp"
        android:layout_marginTop="0dp"
        android:gravity="center|top"
        app:layout_constraintRight_toLeftOf="@+id/status_circle" />

    <View
        android:id="@+id/status_circle"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:background="@drawable/circle"
        android:layout_centerInParent="true"
        android:layout_marginRight="0dp"
        app:layout_constraintRight_toRightOf="@+id/computer_image"
        android:layout_marginTop="0dp"
        app:layout_constraintTop_toBottomOf="@+id/computer_image" />

</android.support.constraint.ConstraintLayout>